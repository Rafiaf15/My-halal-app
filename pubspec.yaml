name: belajar_flutter
description: "A new Flutter project."
publish_to: 'none' # This line prevents accidental publication to pub.dev.

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8  # For iOS icons
  logger: ^1.1.0  # For logging

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0  # Flutter lint rules

flutter:
  uses-material-design: true

  assets:
    - assets/food_background.png
    - assets/logo.png
    - assets/podomoro.jpg
    - assets/buy1.jpg
    - assets/shinjuramen.png
    - assets/marugame.jpg
